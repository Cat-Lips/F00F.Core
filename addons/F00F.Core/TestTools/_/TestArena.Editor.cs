//#if TOOLS
//using Godot.Collections;

//namespace F00F;

//public partial class TestArena
//{
//    public sealed override void _ValidateProperty(Dictionary property)
//    {
//        if (Editor.Hide(property, TestTerrain.PropertyName.Size)) return;
//        if (Editor.Hide(property, TestTerrain.PropertyName.Amplitude)) return;
//        if (Editor.Hide(property, TestTerrain.PropertyName.ShapeType)) return;
//        if (Editor.Hide(property, TestTerrain.PropertyName.Color)) return;
//        if (Editor.Hide(property, TestTerrain.PropertyName.Noise)) return;
//    }
//}
//#endif
